const db = require("../config/database");exports.createOrder = async (request, response) => {    const {id_photo, id_specialist, id_location, client_id, date_implementations} = request.body;    const res = await db.query(        "INSERT INTO orders (photosession_type_id, specialist_id, location_id, data_implementations,client_id) VALUES ($1, $2,$3,$4,$5)",        [id_photo, id_specialist, id_location, date_implementations,client_id]    );    response.status(201).send({        message: "Order added successfully!",        body: {            order: { id_photo, id_specialist, id_location, client_id, date_implementations}        },    });};