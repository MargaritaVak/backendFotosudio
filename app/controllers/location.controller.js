const db = require("../config/database");exports.getLocation = async (request, response) => {    const res = await db.query('SELECT * FROM location ORDER BY id');    response.status(200).json(res.rows);};exports.getLocationById = async (request, response) => {    const id = parseInt(request.params.id);    const res = await db.query('SELECT * FROM location WHERE id = $1', [id]);    response.status(200).json(res.rows);};